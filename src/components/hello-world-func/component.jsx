import React from 'react';
import style from './style.scss';

const HelloWorldFunc = () => (
  <div className={style['hello-world']}>Hello World!!! (from the functional
      component)</div>);

export default HelloWorldFunc;
